/*
 * BtcConverter Messages
 *
 * This contains all the text for the BtcConverter container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BtcConverter';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BtcConverter container!',
  },
});
