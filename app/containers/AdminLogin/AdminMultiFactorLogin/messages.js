/*
 * AdminMultiFactorLogin Messages
 *
 * This contains all the text for the AdminMultiFactorLogin container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminMultiFactorLogin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminMultiFactorLogin container!',
  },
});
