/*
 * VerifyAdminEmailToken Messages
 *
 * This contains all the text for the VerifyAdminEmailToken container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VerifyAdminEmailToken';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VerifyAdminEmailToken container!',
  },
});
