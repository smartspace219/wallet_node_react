/*
 * EmailVerification Messages
 *
 * This contains all the text for the EmailVerification container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EmailVerification';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmailVerification container!',
  },
});
