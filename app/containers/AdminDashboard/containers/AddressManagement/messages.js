/*
 * AddressManagement Messages
 *
 * This contains all the text for the AddressManagement container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddressManagement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddressManagement container!',
  },
});
