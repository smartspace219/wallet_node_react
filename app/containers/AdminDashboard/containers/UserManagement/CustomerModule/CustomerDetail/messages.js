/*
 * CustomerDetail Messages
 *
 * This contains all the text for the CustomerDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CustomerDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomerDetail container!',
  },
});
