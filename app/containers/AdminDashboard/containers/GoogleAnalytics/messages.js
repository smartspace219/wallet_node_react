import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WebsiteInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WebsiteInfo container!',
  },
});