/*
 * CustomerQueries Messages
 *
 * This contains all the text for the CustomerQueries container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CustomerQueries';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomerQueries container!',
  },
});
